fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
sqlmodel==0.0.19
pydantic-settings==2.3.4
redis==5.0.7
rq==1.16.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
scipy==1.13.1

# openmmlab stack - versions pinned to a combination known to work
# together; mmcv/mmengine/mmdet are installed via mim in the
# Dockerfile (see comments there) because mmcv needs a prebuilt wheel
# matched to the torch/CUDA (or CPU) build.
openmim==0.3.9
