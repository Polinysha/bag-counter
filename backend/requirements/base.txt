# --- Core runtime: API + queue + persistence -------------------------
# Installed in every image (full and lite/CI). No CV or ML deps here.
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9   # required by FastAPI/Starlette for multipart file uploads (not imported directly)
sqlmodel==0.0.19
pydantic-settings==2.3.4
redis==5.0.7
rq==1.16.2
